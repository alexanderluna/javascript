# GraphQL Authentication

The client side app that interacts with the backend graphql server. The client
is build on:

- react
- apollo/client
- react-router-dom

## Installation

```bash
yarn install
yarn start
```
